cd pipeline
docker run -it test:pandas 12

docker run -it --rm \
  -e POSTGRES_USER="root" \
  -e POSTGRES_PASSWORD="root" \
  -e POSTGRES_DB="ny_taxi" \
  -v $(pwd)/ny_taxi_postgres_data:/var/lib/postgresql \
  -p 5432:5432 \
  postgres:18

## Open New Terminal
source /workspaces/data-engineering/pipeline/.venv/bin/activate

cd pipeline/
uv add --dev pgcli
uv run pgcli -h localhost -p 5432 -u root -d ny_taxi

## Open New Terminal
source /workspaces/data-engineering/pipeline/.venv/bin/activate

cd pipeline/

uv add --dev jupyter
uv run jupyter notebook


## Ingest Data

uv run python ingest_data.py \
  --pg-user=root \
  --pg-pass=root \
  --pg-host=localhost \
  --pg-port=5432 \
  --pg-db=ny_taxi \
  --target-table=yellow_taxi_trips_2021_01 \
  --year=2021 \
  --month=1 \
  --chunksize=100000
